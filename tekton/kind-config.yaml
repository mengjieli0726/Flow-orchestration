kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 80
    hostPort: 80
    listenAddress: "9.21.53.225" # Optional, defaults to "0.0.0.0"
    protocol: udp # Optional, defaults to tcp
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: worker
  extraPortMappings:
  - containerPort: 80
    hostPort: 80
    listenAddress: "9.21.53.226" # Optional, defaults to "0.0.0.0"
    protocol: udp # Optional, defaults to tcp

